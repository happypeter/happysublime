
shell 编程怎么也是要提提的，哪怕不专门开一集。例子就可以选择我的那些 sync 脚本，让大家真正意识到方便之处，给出大家我的 gist 页面。

环境变量必须讲，可以结合 用户主目录概念，如何更改 shell prompt，如何导出一个命令 $PATH 来讲讲解

.bashrc 必须讲，也可以多聚几个其他例子，.gitconfig .vimrc 让大家理解 Unix 的 KISS，keep it all to readable text

tar zxvf 这个天天用啊，放在手动安装软件那部分讲？还是其他地方？


如果要分章，可以有

1. bash 命令行基础
   - perm
   - nav
   - redirection
   - search
   - ...

2. 必要工具篇
   - apt
   - git vim
   - rsync
